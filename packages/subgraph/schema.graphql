type AvaterList @entity {
  id: ID!
  avater: BigInt!
}

type MaterialList @entity {
  id: ID!
  owner: Bytes!
  dataBytes: Bytes!
  rawData: String
  dataID: BigInt
  configID: BigInt
  remake: Boolean
}
type AvaterList @entity {
  id: ID!
  avater: BigInt!
}

type MaterialList @entity {
  id: ID!
  owner: Bytes!
  rawData: String
  remake: Boolean
  config: TConfig!
  compose: TCompose
}

type TConfig @entity {
  id: ID!
  name: String!
  time: String
  position: String
  zIndex: String
  decode: String
  sort: BigInt
}

type TCompose @entity {
  id: ID!
  composed: BigInt
  composes: [BigInt!]!
}